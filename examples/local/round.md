【聊天摘要 · 本轮】日期 {date}，模式 {mode}。

1) **取数**：读 `{inbox}` 下递归的 `*.txt` / `*.md` / `*.json`。跑没跑以产物为准，不凭印象。
2) **提炼**：挑出对人有用的事，做成面板条目（一句话总结 ＋ 按信息量定的正文）。
3) **回写**：`POST /chat-feed/api/items-patch` 打补丁；新条目带 `isNew`。
4) **回报**：读到了什么、写了哪几条、哪些没做成。
