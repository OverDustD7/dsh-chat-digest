# 管线需要的第三方 Python 包（插件本体是零依赖的 JS；这部分是取数本身，属必要依赖）。
# 建议装进一个 venv：python -m venv venv && venv\Scripts\pip install -r pipeline/requirements.txt
pycryptodome       # Crypto（微信/QQ 数据库解密）
sqlcipher3-binary  # sqlcipher3（读 WCDB / nt_msg.db）
pillow             # PIL（图片处理）
zstandard          # zstandard（微信 4.x 的 zstd 页）
av                 # av（音视频元数据；不装则对应脚本跳过）
